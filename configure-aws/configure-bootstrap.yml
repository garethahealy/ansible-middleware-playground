- hosts: bootstrap
  remote_user: ec2-user
  vars:
    ansible_ssh_private_key_file: configure-bootstrap/files/my_keypair.pem
  roles:
    - name: "Configure bootstrap"
      role: "configure-bootstrap"