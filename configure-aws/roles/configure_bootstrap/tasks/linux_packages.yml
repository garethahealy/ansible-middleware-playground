---
- name: Install packages via dnf
  become: true
  ansible.builtin.dnf:
    name: "{{ item }}"
    state: present
  loop:
    - unzip
    - java-17-openjdk-headless
    - git
    - make
    - awscli2
